{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://thecombine.ai/schemas/route_confirmation.v1.json",
  "title": "Route Confirmation",
  "description": "Operator confirmation or correction of a routing decision",
  "type": "object",
  "additionalProperties": false,
  "required": ["action", "selected_pow_ref"],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "route_confirmation.v1"
    },
    "action": {
      "type": "string",
      "enum": ["confirm", "override"],
      "description": "Whether operator confirms the suggested route or overrides it"
    },
    "selected_pow_ref": {
      "type": "string",
      "minLength": 3,
      "maxLength": 160,
      "description": "The POW reference to use (same as suggestion if confirm, different if override)"
    },
    "confirmation_note": {
      "type": ["string", "null"],
      "maxLength": 500,
      "description": "Optional note explaining the confirmation or override reason"
    },
    "confirmed_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when confirmation was submitted"
    }
  }
}
