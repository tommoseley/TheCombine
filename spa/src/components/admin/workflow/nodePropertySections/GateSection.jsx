import React from 'react';

/**
 * Generic gate node properties.
 * Currently gate nodes only use the common fields (node_id, type, description, flags).
 * This component is a placeholder for future gate-specific properties.
 */
export default function GateSection() {
    return null;
}
